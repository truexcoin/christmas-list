name = "christmas-list"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# KV Namespace binding
# This is used for local development with `wrangler pages dev`
# For production, configure the binding in Cloudflare Pages Dashboard:
# Settings → Functions → KV Namespace Bindings
[[kv_namespaces]]
binding = "KV"
id = "d185102d68fb4a28b1e1ad810d24daed"

# Optional: preview_id for local development
# Uncomment and add your preview namespace ID if you have one
# preview_id = "your-preview-namespace-id"

